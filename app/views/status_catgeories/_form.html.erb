<%= form_for(@status_catgeory) do |f| %>
  <% if @status_catgeory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_catgeory.errors.count, "error") %> prohibited this status_catgeory from being saved:</h2>

      <ul>
      <% @status_catgeory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
