<%= form_for(@status_category) do |f| %>
  <% if @status_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_category.errors.count, "error") %> prohibited this status_category from being saved:</h2>

      <ul>
      <% @status_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
