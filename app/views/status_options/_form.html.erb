<%= form_for(@status_option) do |f| %>
  <% if @status_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_option.errors.count, "error") %> prohibited this status_option from being saved:</h2>

      <ul>
      <% @status_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :status_category_id %><br>
    <%= f.number_field :status_category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
